/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['api.microlink.io', 'www.youtube.com','images.unsplash.com','encrypted-tbn0.gstatic.com'],
  },
};

module.exports = nextConfig;

